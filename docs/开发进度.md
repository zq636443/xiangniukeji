# 开发进度

## 2026-07-30 灵活续租定价、存量合同与外部补录订单调价

- [x] 门店 SKU 支持分别设置首期/原租期价格、整期续租价、正常日续租价、逾期日占用费、宽限小时和整期封顶开关。
- [x] 订单创建时冻结完整续租计费快照，后续修改 SKU 不反向改写历史订单。
- [x] 保留原 `PERIOD` 整期续租模式，历史 SKU、订单和合同默认继续按原固定周期逻辑运行。
- [x] 新增 `DAILY_CAPPED` 按日模式：不足整期按实际天数计费；开启封顶时每个续租周期最多收取整期续租价；关闭封顶时全程按日价计算。
- [x] 到期后的日计费从“预计归还时间 + 宽限小时”开始，任意已开始的计费日按一天计算。
- [x] 按日账单保存计费方式、续租天数和日单价快照，支付后按账单天数精确顺延预计归还时间。
- [x] 归还时自动结算尚未出账的实际使用天数；若已有未付整期账单，仅补收未覆盖的剩余天数和金额，不重复收取整期费用。
- [x] 存量履约订单支持独立“续租调价”记录；降价或履约前修改立即生效，涨价、新增收费、缩短周期/宽限、取消封顶或切换计费模式须用户签署补充协议后生效。
- [x] 原合同、已生成账单和已支付账单保持不变，新规则只作用于下一笔尚未生成的续租账单。
- [x] 新增续租价格调整补充协议模板、主合同/补充协议关联和调价记录关联；后台支持失败后重试生成补充协议。
- [x] 续租账单继续使用 `RENEWAL` / `RENEWAL_RENT` 口径，支付、代扣、逾期、收益分录和月结链路保持兼容。
- [x] 外部补录订单新增独立续租价格快照；新补录单从门店 SKU 冻结规则，V51 自动为已有补录单回填当前门店 SKU 的整期价、日租价、宽限期、封顶和逾期日价。
- [x] 总部与商户 Web 的外部补录订单支持单笔续租调价和调价历史；普通资料编辑或切换 SKU 不会静默覆盖订单已有续租价格。
- [x] 外部补录订单支持按勾选订单、门店、状态、来源平台、门店商品、SKU、起租时间、预计归还时间、订单/客户/手机号/资产关键词批量预览和调价。
- [x] 批量执行必须携带预览命中数量，执行前数量变化时拒绝处理；非进行中订单、规则未变化和已有待确认调价的订单分别跳过或阻止。
- [x] 对客户有利的外部订单变更立即生效；涨价、新增收费、切换计费方式、缩短周期/宽限或取消封顶，未登记客户确认时只生成待确认记录，登记微信/电话/纸质/其他人工确认后生效。
- [x] 外部补录订单完结或提前终止时自动取消尚未确认的调价记录；外部订单仍不自动生成平台账单、代扣、逾期案件或电子合同。

验证记录：

- 全新隔离库 `xniu_rental_test_external_renewal_final` 已从 V1 成功迁移至 V51。
- V50 → V51 存量升级演练已验证旧补录订单可从门店 SKU 回填续租规则。
- `server/rental-api`：105 项测试通过，0 失败、0 错误。
- `admin-web`：`npm run typecheck`、`npm run build` 通过。
- `user-mini`、`merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 通过。

## 2026-07-20 商户与门店账号新建订单权限

- [x] 新增独立权限点 `order.create`，默认不授予任何商户或门店角色。
- [x] 新增账号级直接授权表 `auth_account_permission`，登录态权限由角色权限与账号直接权限合并计算。
- [x] 总部后台 `系统管理 > 账号管理` 新增“新建订单”权限开关，平台管理员可按账号授予或收回。
- [x] 商户 Web 后台和商户支付宝小程序按当前账号权限展示“新建订单”入口。
- [x] 商户创建订单时只允许选择当前授权门店的上架商品、套餐和空闲资产，创建成功后自动生成整单账单计划。
- [x] 新增安全的商户门店商品查询接口，外部补录订单不再复用总部商品接口。
- [x] 普通订单创建入口增加账号类型隔离，商户账号不能绕过总部或消费者接口创建订单。
- [x] 新增集成测试，覆盖权限授予/收回、无权限拦截、门店范围拦截、成功创建与自动出账。

## 2026-07-19 外部补录订单台账

- [x] 新增 [外部补录订单台账设计](./外部补录订单台账设计.md)，明确该能力是“存量订单跟踪台账”，与正式租赁订单状态机分开建模。
- [x] 后端新增 `external_rental_order`、`external_rental_order_log` 两张表，支持来源平台、外部单号、客户信息、门店商品、套餐、资产绑定、状态流转和日志沉淀。
- [x] 后端新增总部 / 商户补录订单接口：列表、详情、新建、正常完结、提前终止。
- [x] 创建补录订单时已打通真实资产占用逻辑：校验 SKU 资产要求、校验空闲状态、拦截与正式订单/其他补录订单冲突、自动写入资产状态日志与位置日志。
- [x] 结束补录订单时已支持选择归还门店，并自动把资产回收到指定门店，支持跨店归还后的资产自动调拨。
- [x] 总部后台 `交易履约` 与商户后台 `门店运营` 已新增 `外部补录订单` 页面，支持筛选、搜索、新建、详情、完结和提前终止。
- [x] 资产详情租赁记录已并入补录订单历史，车架/电池详情页现在能同时查看正式订单与补录订单轨迹。
- [x] 总部后台与商户后台已新增“批量导入补录订单”入口，支持按行粘贴历史订单数据并返回逐行导入结果。
- [x] 后端新增集成测试 `ExternalRentalOrderIntegrationTests`，覆盖补录订单创建、资产占用、提前终止、资产归还与跨店调拨闭环。

## 2026-07-06 系统管理一期

- [x] 新增 [闲鱼核销业务规则](./闲鱼核销业务规则.md)，明确总部下发闲鱼核销码、用户到店验码、签单费支付、平台内部核销完成和后续履约的完整链路。
- [x] 核销模块已从“抖音/美团”升级为“平台核销”模型，新增 `XIANYU` 来源平台，统一复用验码、签单费账单、履约订单和异常处理能力。
- [x] 总部后台 `团购核销` 已新增“下发闲鱼核销码”入口，可选择门店商品、套餐、成交金额并预先下发核销码，避免闲鱼场景变成任意输码即可通过。
- [x] 用户版小程序已补闲鱼核销来源选项；商户端和总部后台已补闲鱼平台展示与筛选，门店可按统一核销协助视角查看闲鱼来源记录。

- [x] 新增 [系统管理权限体系设计](./系统管理权限体系设计.md)，明确系统管理 7 个子模块、角色模型、权限点模型和数据范围规则。
- [x] 统一 Web 后台完成双模式入口改造：同一套 `admin-web` 内新增 `总部登录 / 商户登录` 双入口，并按账号类型自动进入总部模式或商户模式。
- [x] 商户模式已接入安全的 `/api/merchant/*` 接口，打通门店切换、商户工作台、门店订单、资产、逾期、收益等核心页面，避免商户误用总部全局接口。
- [x] 统一 Web 后台新增出资方模式：支持 `出资方登录` 入口，并打通 `/api/investor/assets`、`/api/investor/settlement/income/entries`，可查看自己名下资产和收益分成。
- [x] 出资方模式新增资产详情下钻：后端补齐 `/api/investor/assets/{id}/detail`，前端可查看资产租赁记录、账单记录和维修记录。
- [x] `出资方管理 > 新建出资方` 支持同步创建出资方账号，创建后自动绑定对应 `investorId`，可直接登录出资方工作台。
- [x] 本地联调已验证历史出资方补账号闭环：缺失账号的出资方可补建 `INVESTOR` 账号，并通过 `/api/auth/admin/login` 正常登录出资方工作台。
- [x] 新增 [结算中心业务规则](./结算中心业务规则.md)，明确自然月结算、实收确认、跨月补缴、维保扣减、月结单状态和三方查看口径。
- [x] 后端新增月结中心核心表：`settlement_statement`、`settlement_statement_line`，按自然月沉淀商户门店月结单与出资方月结单。
- [x] 后端新增月结中心接口：总部可生成月结单、查看月结概览、查询月结单与明细、更新月结状态；商户与出资方可查看自己的月结单与明细。
- [x] 总部后台 `分润结算` 页面新增“月结中心”，支持按月份生成月结单、查看月结概览、月结单列表和明细。
- [x] 商户工作台、出资方工作台补齐最近月结单与月结明细视图，形成“收益分录 + 月结单”双视角。
- [x] 分润规则升级为 `PROFIT_V2`：以实际核销/结算金额为基数，先扣渠道 `5%`、租赁平台 `3%`，剩余金额按门店运营 `15%`、维修基金 `10%`、渠道引流 `20%`、出资方 `55%` 分配。
- [x] 新版快照保存渠道、计算版本、规则优先级和六项金额；历史快照保留 `LEGACY_V1` 结果，不因新规则上线而重算。
- [x] 新增 [月结联调 SQL](./database/settlement-demo-seed.sql)，可一键补齐 `2026-07` 的已支付账单、资产使用轨迹和维修扣减数据，用于真实生成商户/出资方月结单。
- [x] 修正月结中心出资方多资产同归属的 rounding 误差：同一出资方名下若同时使用车架和电池，改为先按出资方汇总租金基数，再计算毛收益，避免月结多算 `0.01`。
- [x] 本地已真实生成 `2026-07` 月结单：商户应结 `117.75`，出资方应结 `188.60`，后台接口与数据库明细已核对一致。
- [x] 新增 [配件仓与维保结算规则](./配件仓与维保结算规则.md)，明确平台仓、门店仓、门店采购入库、退仓回收、维修责任归因和月底清分口径。
- [x] 后端启动配件仓二期改造：新增平台采购价、门店回收价、门店库存按商户/门店归属、维修记录沉淀门店补偿金额/出资方扣减金额/用户追偿金额。
- [x] 后台配件仓页面启动改造：支持平台入库、门店采购、门店退仓、门店库存查看和扩展库存流水类型展示。
- [x] 后台 `配件仓库` 页面完成结构化升级：拆分为 `配件种类 / 门店库存 / 门店采购 / 门店退仓 / 库存流水` 五个视图，不再依赖行内弹窗才能查看采购、退仓和门店库存台账。
- [x] 商户后台补齐资产维保模块：新增 `门店配件`、`维修记录` 菜单，`门店资产` 支持资产详情下钻查看租赁记录与维修记录；后端补齐商户侧资产详情、维修记录、配件库存与库存流水接口。
- [x] 商户支付宝小程序补齐维保与配件入口：支持从资产卡片发起维修、按门店库存选择消耗配件、查看门店维修记录、查看门店配件库存与库存流水；门店间配件调拨统一收口到商户 Web 后台处理。
- [x] 商户 Web 后台 `门店配件` 已补齐正式的配件调拨入口，支持同一商户下多门店之间发起配件调拨，并同步刷新库存与库存流水。
- [x] 后端新增系统管理一期接口：账号列表、角色列表、权限列表、账号状态调整、账号角色调整、账号门店范围调整。
- [x] 总部后台 `系统管理 > 账号管理` 新增“新增账号”按钮和弹窗，支持创建平台账号、商户账号、出资方账号，并按角色联动选择商户、出资方和授权门店。
- [x] `商户管理 > 新建商户` 支持同步创建商户主账号，便于建档后直接登录查看门店、订单、资产和收益数据。
- [x] 总部后台 `系统管理 > 账号管理` 补齐账号详情、基础资料编辑、密码重置，并在账号列表直接提供商户成员数据范围调整入口。
- [x] 后端补齐系统账号详情、基础信息修改、后台重置密码接口，并新增集成测试覆盖创建、编辑、重置密码闭环。
- [x] 新增系统管理权限点：`auth.account.read`、`auth.account.write`、`auth.role.read`、`auth.role.write`、`auth.permission.read`、`auth.scope.read`、`auth.scope.write`、`auth.audit.read`。
- [x] 新增商户侧角色：`STORE_OPERATOR`、`WAREHOUSE_STAFF`。
- [x] 商户登录放开新角色和维修/仓库角色登录，便于后续成员按岗位登录。
- [x] 总部后台新增 `系统管理 > 账号权限` 页面，可统一查看账号、角色、权限，并调整账号角色、停用状态、门店范围。

验证记录：

- `server/rental-api`：`mvn test` 已通过，Flyway 已验证并执行到 23 个迁移。
- `admin-web`：`npm run typecheck`、`npm run build` 已通过。

## 2026-06-29 业务漏洞补强

- [x] 资产出资方与门店运营方职责拆分加固：出资方只按名下资产产生收益，商户/门店负责运营、履约、维修和配件消耗。
- [x] 新增订单资产使用轨迹 `order_asset_usage`，取车开始使用记录，换车关闭旧资产并开启新资产，归还关闭使用记录，后续收益归属优先按真实使用轨迹计算。
- [x] 配件仓库升级为门店库存：新增 `store_spare_part_stock`，入库、调整、维修消耗均按门店扣减，避免 A 门店消耗 B 门店库存。
- [x] 维修记录新增成本承担方：支持用户、出资方、商户、平台，并记录承担方 ID；默认由资产出资方承担。
- [x] 维修登记限制为资产当前所在门店，防止维修记录、配件库存和资产位置错配。
- [x] 分润生成优先读取订单资产使用轨迹；无轨迹的历史数据回退到订单分润快照中的车架/电池资产；出资方运营手续费、维保费、出资方净收益均按真实使用资产重算。
- [x] 后台配件仓库支持选择门店入库/调整，库存流水展示门店 ID。
- [x] 后台资产维修登记支持关联订单、选择成本承担方，资产详情维修记录展示承担方。

验证记录：

- `server/rental-api`：`mvn test` 已通过，Flyway 已验证 21 个迁移，本地后端已在 `8090` 使用新代码启动，`/actuator/health` 返回 `UP`。
- `admin-web`：`npm run typecheck`、`npm run build` 已通过，本地前端 `5173` 返回 `200`。

## 当前进度

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 模块 1：工程基础与环境 | 已完成 | 三个独立前端项目、后端骨架、基础构建验证已完成。 |
| 模块 2：账号、角色、权限 | 已完成 | 已完成账号密码登录、支付宝登录占位接口、Token 会话/刷新、RBAC、门店范围、三端登录入口；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 3：商户、门店、员工 | 已完成 | 已完成商户、门店、员工账号、门店授权范围、门店二维码内容和三端入口；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 4：出资方与资产基础台账 | 已完成 | 已完成出资方、车架/电池资产、归属历史、位置历史、状态日志、后台资产台账、商户端库存查看；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 5：商品、SKU、门店 SKU、套餐 | 已完成 | 已完成商品分类、全局 SKU、套餐模板、门店 SKU、门店套餐价格、签单费、租期/账单日配置、批量上架和用户端门店商品展示；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 6：分润规则与规则快照 | 已完成 | 已完成平台/SKU/门店/门店 SKU 与渠道组合规则、优先级命中、新版六项分润、规则预览、版本化快照及历史兼容。 |
| 模块 7：订单状态机 | 已完成 | 已完成订单主表、订单明细、操作日志、订单创建、状态机流转、取消和异常标记；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 8：账单模型与账单生成 | 已完成 | 已完成账单主表、账单明细、生成批次、账单操作日志、首期账单、周期账单、整单账单计划、逾期账单、关闭账单和后台账单管理；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 9：支付宝支付基础能力 | 已完成 | 已完成支付单、支付宝交易创建、支付回调验签、幂等入账、主动查询、退款接口、后台支付查询、回调记录和用户小程序调起支付；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 10：支付宝签约与扣款任务 | 已完成 | 已完成支付宝签约协议、签约通知、协议查询、解约、到期账单扣款任务、失败重试、扣款批次和后台签约扣款管理；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 11：逾期订单汇总 | 已完成 | 已完成逾期台账、扣款失败自动生成/更新逾期、补缴成功自动解决、催缴状态备注、后台逾期汇总和商户端门店逾期处理；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 12：资产交接、绑定、更换、归还 | 已完成 | 已完成取车资产绑定、交接单、更换单、换车架/换电池、归还单、资产状态流转、订单结束和后台/商户端履约入口；后端已完成本地编译、测试、打包和健康检查验证。 |
| 模块 13：支付宝用户版小程序核心链路 | 已完成 | 已完成扫码/输入门店码、门店商品、套餐下单、账单计划、主动支付、签约代扣、我的订单和我的账单闭环。 |
| 模块 14：支付宝商户版小程序核心链路 | 已完成 | 已完成门店订单看板、订单筛选检索、订单详情、账单查看、分润快照、库存选资产和取车/更换/归还履约联动。 |
| 模块 15：总部后台业务管理 | 已完成 | 已完成总部经营工作台，聚合订单、账单、资产、支付、逾期和扣款失败队列，并保留各业务管理页。 |
| 模块 16：芝麻免押/资金授权 | 已完成 | 已完成支付宝资金授权冻结、状态查询、授权扣费、解冻、撤销、回调记录、用户端授权入口和后台授权管理；芝麻免押可复用授权单模型后续扩展。 |
| 模块 17：OCR、实名、电子合同 | 已完成 | 已完成身份证图片提交、实名确认脱敏保存、合同模板、订单合同生成、签署链接、签署回调、PDF 归档和后台实名合同管理。 |
| 模块 18：平台核销 | 已完成 | 已完成抖音、美团、闲鱼来源的内部核销记录、核销准备、验码、签单费账单、支付后核销、失败/异常处理和三端操作入口；真实第三方自动核销已预留服务端网关适配点。 |
| 模块 19：分润结算与出资方收益 | 已完成 | 已完成按订单分润快照幂等生成渠道扣点、平台扣点、门店运营、门店维修分润、渠道引流、出资方六类收益台账；门店运营与门店维修均计入门店应结算，并支持续租租金进入月结计算。 |
| 模块 20：对账、导出、审计、上线加固 | 已完成 | 已完成平台内对账批次/差异、导出任务、操作审计日志、后台运维审计页和部署备份/监控要求；支付宝账单自动下载解析需上线环境配置证书密钥后接定时任务。 |

## 本地环境验证

- JDK 21 已安装到 `~/.local/devtools/jdk-21`，Maven 3.9.16 已安装到 `~/.local/devtools/maven-3.9.16`，并已写入 `~/.zshrc`。
- 本地依赖使用 Docker 启动：`xniu-mysql` 映射 `localhost:3310 -> 3306`，数据库 `xniu_rental`，账号 `root/root`；`xniu-redis` 映射 `localhost:6380 -> 6379`。
- 后端已通过 `mvn test` 和 `mvn package`，Flyway 已成功执行 16 个迁移，打包产物为 `server/rental-api/target/rental-api-0.1.0.jar`。
- 后端曾以前台方式使用 `SERVER_PORT=8090` 启动并验证 `GET /api/health` 返回 `200` 和 `status=UP`。Codex 后台子进程会在命令结束后被清理，因此本次未保持后台服务常驻。
- 后端新增业务集成测试，覆盖创建租赁订单、生成整单账单计划、账单金额合计校验、取车绑定资产、归还结束订单、收益台账生成和幂等生成。
- 资产取车状态校验已加固：只有 `PENDING_PICKUP` 订单允许取车绑定；归还资产只允许履约中的订单，避免未支付/未实名/未签约订单直接进入租赁履约。

## 模块 2 验收项

- [x] 后端具备账号、角色、权限、会话、门店权限范围数据结构。
- [x] 支持后台账号密码登录。
- [x] 支持商户版账号密码登录。
- [x] 支持用户版支付宝登录占位接口。
- [x] 支持获取当前登录用户和权限范围。
- [x] 支持 Token 刷新和退出登录。
- [x] 后台、用户版小程序、商户版小程序都有登录入口骨架。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 13 验收项

- [x] 用户版小程序支持支付宝授权登录，并使用用户 Token 调用后续接口。
- [x] 用户可输入门店码或扫描门店二维码内容。
- [x] 用户可按门店码查询当前门店已上架商品。
- [x] 用户可查看商品资产要求、签单费承担方和可选套餐。
- [x] 用户可选择套餐创建自己的订单，后端按当前登录用户绑定订单。
- [x] 用户下单后自动生成整单账单计划，首期账单包含租金、签单费和押金。
- [x] 用户可查看自己的订单列表，并切换当前订单查看详情。
- [x] 用户可查看自己的账单列表，也可按当前订单查看账单。
- [x] 用户可对首期、周期、逾期、扣款失败等未结清账单主动发起支付宝小程序支付。
- [x] 用户支付时后端创建支付宝交易，前端调用 `my.tradePay`。
- [x] 用户可基于当前订单发起支付宝代扣协议签约。
- [x] 后端用户订单、用户账单接口均校验当前登录用户，只能查看或生成自己的数据。
- [x] 用户版小程序首页形成“扫码门店 -> 选商品套餐 -> 下单 -> 账单 -> 支付/签约 -> 我的订单账单”的闭环。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对用户订单、用户账单、支付宝交易和支付宝签约接口链路。

## 模块 14 验收项

- [x] 商户版小程序支持登录后查看授权门店。
- [x] 商户切换门店后自动加载门店订单、资产库存和逾期订单。
- [x] 商户可按订单状态筛选门店订单。
- [x] 商户可按订单号、用户 ID、订单 ID 搜索当前门店订单。
- [x] 商户可进入订单详情，查看订单状态、租期、金额、资产绑定和订单明细。
- [x] 商户可查看当前订单账单列表，包含账单类型、期数、状态、到期时间和金额。
- [x] 商户可查看订单分润快照；新版快照展示实际结算金额、渠道/平台扣点、门店运营、维修基金、渠道引流和出资方分润，历史快照继续展示旧字段。
- [x] 后端新增商户订单列表、订单详情、订单账单、订单分润快照接口。
- [x] 商户订单接口强制校验门店授权范围，不能跨门店读取订单、账单或分润。
- [x] 商户可从门店空闲资产中一键填入车架、电池或更换资产。
- [x] 商户取车绑定后同步刷新订单、资产和逾期数据。
- [x] 商户更换车架或电池后同步刷新订单、资产和逾期数据。
- [x] 商户归还并结束订单后同步刷新订单、资产和逾期数据。
- [x] 商户可查看门店逾期订单，并更新催缴状态。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对商户订单、账单、分润快照和资产履约接口链路。

## 模块 15 验收项

- [x] 后台保留商户门店、资产出资方、商品套餐、订单账单、分润结算等业务菜单。
- [x] 后台新增经营工作台作为首页。
- [x] 工作台聚合订单、账单、资产、逾期、支付和扣款失败数据。
- [x] 工作台展示租赁中订单、待取车订单、逾期/待补缴订单、待收账单金额、逾期未收金额、累计已支付金额、空闲资产、租赁中资产和异常/维修资产指标。
- [x] 工作台展示待取车订单队列，便于总部跟进门店取车履约。
- [x] 工作台展示待收账单队列，覆盖待支付、逾期和扣款失败账单。
- [x] 工作台展示逾期催缴队列，包含未收金额、失败次数和催缴状态。
- [x] 工作台展示扣款失败队列，包含扣款号、账单、金额、重试次数和失败原因。
- [x] 工作台所有数据来自现有真实后台接口，没有使用静态假数据。
- [x] 后台首页支持一键刷新经营数据。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；模块 15 使用既有后台接口聚合，新增前端工作台已完成构建验证。

## 模块 16 验收项

- [x] 后端具备资金授权订单表，保存订单、用户、授权金额、冻结金额、扣费金额、解冻金额、支付宝授权号、冻结操作号和状态。
- [x] 后端具备资金授权操作流水表，记录冻结、查询、扣费、解冻、撤销每一步。
- [x] 后端具备资金授权回调表，保存支付宝通知、验签结果、处理结果和失败原因。
- [x] 支持用户端为自己的订单发起支付宝资金授权冻结。
- [x] 用户端调用后端创建授权单后，使用支付宝小程序运行时拉起 `orderStr` 完成授权。
- [x] 支持支付宝资金授权异步通知验签，通知地址已加入登录鉴权白名单。
- [x] 支持后台查询并同步支付宝资金授权状态。
- [x] 支持后台基于授权号进行授权扣费。
- [x] 授权扣费可关联账单，成功后生成支付单、标记账单已支付、累加订单已付金额，并自动解决对应逾期案件。
- [x] 支持后台解冻剩余授权额度。
- [x] 支持后台撤销授权。
- [x] 后台新增“资金授权”页，支持授权列表、状态筛选、查询同步、扣费、解冻、撤销、操作流水和回调记录。
- [x] 支付宝 SDK 已静态核对存在资金授权冻结、查询、解冻、撤销和授权转支付所需类。
- [x] `application.yml` 已补充资金授权回调、冻结产品码、授权场景码、转支付产品码和转支付场景配置。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已下载并静态核对 `com.alipay.sdk:alipay-sdk-java:4.40.865.ALL` jar 中存在资金授权相关 SDK 类。

## 模块 17 验收项

- [x] 后端具备实名记录表，保存身份证正反面图片、OCR 状态、实名状态、脱敏姓名、脱敏证件号和证件哈希。
- [x] 用户端可为自己的订单提交身份证正反面图片 URL。
- [x] 用户端可确认实名信息，后端只保存脱敏信息和身份证哈希，不明文保存身份证号。
- [x] 订单处于待实名时，实名完成后自动流转到待签约。
- [x] 后端具备合同模板、订单合同和合同签署通知表。
- [x] 后台可创建合同模板，并启用/停用模板。
- [x] 后台可按订单生成合同，合同变量来自订单和实名信息。
- [x] 合同模板明确不写入车架号、电池号，资产以取车交接单、更换单、归还单为准。
- [x] 后台可发起签署、归档 PDF，并查看签署回调记录。
- [x] 用户端可查看订单合同、打开/复制签署链接，并确认签署。
- [x] 签署完成后，订单从待签约流转到待免押/资金授权。
- [x] 合同回调地址已加入登录鉴权白名单。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对实名、合同、回调、模板启停和订单状态流转链路。

## 模块 18 验收项

- [x] 后端具备平台核销记录表，保存来源平台、券码、用户、门店、门店 SKU、套餐、订单、签单费账单、核销状态、第三方或内部流水、失败原因和异常原因。
- [x] 后端支持用户端券码录入、验券准备、确认验券和支付签单费后核销。
- [x] 验券成功后才生成平台订单和签单费账单。
- [x] 线上团购订单的账单只收签单费，不重复收取用户已在抖音/美团支付的租金。
- [x] 签单费账单未支付时，后端阻止执行第三方消费核销。
- [x] 核销成功后，订单从待支付流转到待实名。
- [x] 核销失败时，核销记录进入失败状态，订单不进入有效租赁履约状态。
- [x] 后端预留抖音/美团自动核销网关配置与服务端适配点，默认使用模拟/人工模式跑通业务闭环；拿到开放平台资质、密钥、签名规则和字段文档后接入真实适配器。
- [x] 用户版小程序支持选择套餐后录入或扫码核销码、选择抖音/美团/闲鱼、完成核销准备、生成签单费账单并支付后核销。
- [x] 商户版小程序支持查看当前门店核销记录、筛选状态、查看失败原因并标记异常。
- [x] 总部后台支持核销记录列表、平台/状态筛选、查看签单费账单/订单和标记异常。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对核销记录、签单费账单、订单创建、支付前拦截、消费核销和异常处理链路。

## 模块 19 验收项

- [x] 后端具备收益台账表，保存订单、分润快照、门店、收益方、收益类型、金额、结算状态和结算时间。
- [x] 后端支持按订单分润快照幂等生成收益明细，重复生成不会重复入账。
- [x] 收益明细覆盖门店办单费、门店租金分成、平台租金分成、出资方运营手续费、维保费和出资方净收益。
- [x] 出资方收益按订单快照中的车架/电池资产归属拆分；多资产多出资方时按资产权重拆分。
- [x] 未绑定资产时，出资方收益生成待归属记录，便于后续补齐资产后处理。
- [x] 后台可查看收益台账，按订单生成收益，并将收益明细标记为已结算或冻结。
- [x] 后台收益台账可区分门店、平台和出资方收益方。
- [x] 商户版小程序可查看当前门店收益明细，并按待结算/已结算/已冻结筛选。
- [x] 后端提供商户收益查询接口，并校验门店权限。
- [x] 后端提供出资方收益查询接口，出资方账号只能查询自己名下收益。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：模块 19 未改动用户端，本轮此前 `npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对收益台账生成、幂等、状态变更、商户门店权限和出资方隔离链路。

## 模块 20 验收项

- [x] 后端具备操作审计日志表，记录账号、接口、方法、状态码、结果、错误、IP 和 User-Agent。
- [x] 后端通过统一拦截器记录所有已登录的 `POST/PUT/PATCH/DELETE` 操作。
- [x] 后端具备导出任务表，支持订单、账单、收益台账、核销记录等导出任务登记。
- [x] 后端具备对账批次表和对账差异表。
- [x] 后台可创建对账批次，平台支付金额可与录入的渠道账单金额比较，金额不一致时生成差异批次和差异记录。
- [x] 后台可查看对账批次、导出任务和操作审计日志。
- [x] 后台新增“运维审计”菜单，仅 `system.admin` 可见。
- [x] 部署文档补充数据库备份、Redis 持久化、健康检查、扣款失败/支付回调失败/对账差异告警要求。
- [x] 支付宝账单自动下载解析的代码入口已通过对账批次模型预留，生产环境需要配置支付宝证书、密钥、账单日期调度后接入定时任务。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：模块 20 未改动商户端，本轮此前 `npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：模块 20 未改动用户端，本轮此前 `npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对审计拦截器、导出任务、对账批次、差异生成和后台权限链路。

## 模块 3 验收项

- [x] 总部可创建、编辑、停用商户。
- [x] 总部可创建、编辑、停用门店。
- [x] 门店包含地址、营业时间、经纬度、状态。
- [x] 总部可创建商户员工账号并授权门店范围。
- [x] 每个门店能生成独立二维码内容。
- [x] 商户端登录后只能看到授权门店并可选择当前门店。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 4 验收项

- [x] 后端具备出资方管理。
- [x] 后端具备车辆/车架资产台账。
- [x] 后端具备电池资产台账。
- [x] 资产可绑定出资方并记录归属历史。
- [x] 资产可记录当前所在商户和门店，并支持门店调拨。
- [x] 资产状态支持空闲、租赁中、待检修、维修中、已报废、已售出、异常。
- [x] 资产状态变更有日志。
- [x] 资产具备采购金额、固定维保费、报废残值字段。
- [x] 后台可维护出资方、资产入库、调拨门店、变更状态、变更出资方、查看日志。
- [x] 商户端可查看授权门店资产库存。
- [x] 出资方账号可通过后台入口查看自己名下资产，后端强制限制只能查询自己的 `investor_id`。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 5 验收项

- [x] 后端具备全局商品分类。
- [x] 后端具备全局 SKU。
- [x] 后端具备套餐模板。
- [x] 后端具备门店 SKU。
- [x] 门店 SKU 可配置门店价格、签单费、签单费承担方。
- [x] 套餐可配置自定义租期、总期数、每期金额、账单日规则。
- [x] SKU 可配置是否需要车架资产、是否需要电池资产。
- [x] SKU 可配置是否支持跨店归还。
- [x] 后台支持商品分类、SKU、套餐、门店上架和批量上架。
- [x] 用户端输入或扫码门店码后可查看该门店已上架商品和套餐详情。
- [x] 门店未上架或已下架 SKU 不在用户端展示。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 6 验收项

- [x] 支持平台默认分润规则。
- [x] 支持 SKU 默认分润规则。
- [x] 支持门店默认分润规则。
- [x] 支持门店 SKU 独立分润规则。
- [x] 支持按渠道叠加适用范围，覆盖平台直租、抖音、美团、闲鱼及后续扩展渠道。
- [x] 支持从实际核销/结算金额扣除渠道扣点和租赁平台扣点。
- [x] 支持将扣点后的可分配金额按门店运营、门店维修、渠道引流、出资方四项比例拆分，四项比例必须合计 `100%`。
- [x] 默认规则为渠道 `5%`、平台 `3%`；剩余金额按门店运营 `15%`、门店维修 `10%`、渠道引流 `20%`、出资方 `55%` 分配，其中门店合计取得剩余金额的 `25%`。
- [x] 日常维修不再从出资方收益扣减，由门店维修分润覆盖；平台兜底可单独补门店。
- [x] 一张订单只允许对应一个出资方，车架和电池属于不同出资方时必须拆单结算。
- [x] 支持规则生效时间、失效时间和启停。
- [x] 支持按范围精确度、规则优先级、生效时间命中规则，并支持渠道与门店商品组合规则覆盖默认规则。
- [x] 支持规则预览，展示结算基数、两项扣点、剩余可分配金额和四项分配结果。
- [x] 支持生成版本化规则快照，保存渠道、金额、比例、命中规则和资产信息；历史快照不追溯重算。
- [x] 后台支持分润规则配置、规则预览、启用/停用、快照查看。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 7 验收项

- [x] 后端具备订单主表。
- [x] 后端具备订单明细表。
- [x] 后端具备订单操作日志。
- [x] 支持创建订单，并保存商品、套餐、资产、金额和分润快照 ID。
- [x] 支持订单状态机集中校验。
- [x] 支持状态流转：待支付、待实名、待签约、待免押、待核销、待取车、租赁中、待还车、逾期、待补缴、完成。
- [x] 支持订单取消，并限制只有取车前状态可取消。
- [x] 支持订单异常标记，并阻止终态订单继续标记异常。
- [x] 状态变更、取消、异常均记录操作日志。
- [x] 后台支持订单列表、明细展开、操作日志查看、新建订单、状态流转、取消、异常标记。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 8 验收项

- [x] 后端具备账单主表、账单明细表、账单生成批次表。
- [x] 后端具备账单操作日志表，生成和关闭账单均记录状态流转。
- [x] 支持首期账单生成，首期包含首期租金、签单费、押金。
- [x] 支持周期账单生成，按订单自定义总期数拆分租金。
- [x] 支持整单账单计划生成，已存在的同类型同期期数不会重复生成。
- [x] 支持逾期账单生成，逾期金额单独形成账单明细。
- [x] 账单租金拆分采用最后一期补差，保证各期租金合计等于订单租金。
- [x] 支持按状态、订单、门店筛选账单。
- [x] 支持手动关闭未支付账单，并禁止关闭已支付账单。
- [x] 后台“订单账单”入口已拆分订单和账单标签页，支持账单列表、明细展开、日志查看、批次查看、整单生成、单笔生成和关闭账单。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 9 验收项

- [x] 后端具备支付单表，支付单和账单、订单关联。
- [x] 后端具备支付宝回调记录表，保存通知 ID、交易状态、验签结果、处理结果和失败原因。
- [x] 接入支付宝 Java SDK，配置项支持 `appId`、应用私钥、支付宝公钥、notifyUrl、网关地址。
- [x] 支持支付宝 `alipay.trade.create` 创建小程序支付交易，并返回 `tradeNo` 给用户小程序。
- [x] 用户小程序支持输入账单 ID 创建支付交易，并调用 `my.tradePay` 发起支付宝支付。
- [x] 支持支付宝异步通知验签，notify 地址已加入登录鉴权白名单。
- [x] 支付回调按 `notify_id` 去重，支付单已支付时不会重复入账。
- [x] 支付成功后同步更新支付单、账单已付金额、账单状态和订单累计已付金额。
- [x] 回调金额和支付单金额不一致时不入账，并记录失败原因。
- [x] 支持后台支付单列表、回调记录列表、主动查询同步和退款操作。
- [x] 支持支付宝 `alipay.trade.query` 主动查询支付状态。
- [x] 支持支付宝 `alipay.trade.refund` 退款接口。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已通过 Maven Central metadata 确认 `com.alipay.sdk:alipay-sdk-java:4.40.865.ALL` 存在，并静态确认 jar 内包含所用支付宝 SDK 类。

## 模块 10 验收项

- [x] 后端具备支付宝扣款协议表，保存平台外部协议号、支付宝协议号、用户、订单、门店、协议状态和单笔扣款上限。
- [x] 后端具备支付宝签约通知表，记录验签结果、处理结果和失败原因。
- [x] 后端具备扣款批次表和扣款记录表，支持记录每次任务、每笔账单扣款状态、重试次数、下次重试时间和失败原因。
- [x] 支持用户端发起支付宝个人协议页面签约请求。
- [x] 支持支付宝签约通知验签，签约通知地址已加入登录鉴权白名单。
- [x] 支持后台查询协议状态并同步协议号、签约时间、有效期。
- [x] 支持后台解约。
- [x] 支持每日定时扫描到期未支付账单。
- [x] 支持后台手动触发到期账单扣款任务。
- [x] 扣款任务按订单查找已签约协议，并通过 `agreement_no` 调用支付宝协议扣款。
- [x] 同一账单存在成功或处理中扣款记录时会跳过，避免重复扣款。
- [x] 扣款成功后更新支付单、账单、订单累计已付金额和账单操作日志。
- [x] 扣款失败后记录失败原因、增加重试次数、设置下次重试时间，并将账单标记为失败，供模块 11 逾期汇总处理。
- [x] 后台新增“签约扣款”页，支持协议列表、协议通知、扣款记录、扣款批次、协议查询、解约和手动执行扣款。
- [x] 用户小程序新增自动扣款签约入口。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`；已静态核对支付宝 SDK 中签约、协议查询、解约和 `alipay.trade.pay` 所用类与方法存在。

## 模块 11 验收项

- [x] 后端具备逾期订单汇总表，按账单唯一生成逾期案件。
- [x] 后端具备催缴日志表，记录催缴状态、操作人和备注。
- [x] 逾期案件包含统计月份、订单、账单、用户、商户、门店、门店商品、SKU、逾期金额、未补缴金额、失败次数和最近失败原因。
- [x] 扣款失败、无有效协议、超过协议单笔上限都会生成或更新逾期案件。
- [x] 逾期案件生成后订单会进入待补缴状态。
- [x] 用户主动补缴或协议扣款成功后，逾期案件自动标记为已解决。
- [x] 总部后台支持按月份、逾期状态、催缴状态查询逾期汇总。
- [x] 总部后台支持催缴备注和 CSV 导出逾期明细。
- [x] 商户端支持查看授权门店逾期订单。
- [x] 商户端支持更新催缴状态：已联系、承诺付款、已解决。
- [x] 后台和商户端共用同一套逾期接口与数据源。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。

## 模块 12 验收项

- [x] 后端具备资产取车/归还交接单表。
- [x] 后端具备资产更换单表。
- [x] 支持商户端按订单取车绑定车架、绑定电池，二者可任选其一或同时绑定。
- [x] 取车绑定后资产状态变为租赁中。
- [x] 取车绑定后订单进入租赁中，并写入租期开始和预计归还时间。
- [x] 支持更换车架或电池，并同步更新订单当前资产 ID。
- [x] 更换资产后原资产可回到空闲、待检修或异常状态，新资产变为租赁中。
- [x] 更换资产生成更换单，并保留原资产、新资产、资产类型、处理结果和备注。
- [x] 支持归还资产并结束订单。
- [x] 归还后资产回到空闲、待检修或异常状态。
- [x] 归还后生成归还交接单，并将订单标记为已完成。
- [x] 资产状态变化均写入资产状态日志。
- [x] 订单履约动作均写入订单操作日志。
- [x] 后台资产模块新增履约凭证页，可查看交接单和更换单。
- [x] 商户版小程序新增订单资产履约入口，支持取车绑定、更换资产、归还结束订单。

验证记录：

- `admin-web`：`npm run typecheck`、`npm run build` 已通过。
- `merchant-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `user-mini`：`npm run typecheck`、`npm run build:mp-alipay` 已通过。
- `server/rental-api`：JDK 21 和 Maven 3.9.16 已安装；后端已通过 `mvn test`、`mvn package`，并使用 Docker MySQL/Redis 验证 Flyway 16 个迁移和 `/api/health`。
