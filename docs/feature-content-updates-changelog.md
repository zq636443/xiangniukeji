# feature/content-updates 分支改动清单

## 1. 分支信息

- 分支：`feature/content-updates`
- 基线分支：`main`
- 基线提交：`ffc1391 chore: initialize xniu rental platform`
- 远程仓库：`https://github.com/zq636443/xiangniukeji.git`
- 整理日期：`2026-07-22`
- 业务代码及既有文档改动范围：156 个文件
- 代码量变化（不含本清单）：约 10790 行新增、1058 行删除

## 2. 登录与账号权限

### 2.1 统一运营后台登录

- 取消登录页独立的“总部登录”入口。
- 登录页仅保留“商户登录”和“出资方登录”。
- 商户登录改为统一运营后台登录接口 `/api/auth/workspace/login`。
- 平台管理员、财务账号登录后自动进入总部管理后台。
- 商户老板、门店经理、门店运营、门店员工、维修人员、仓库人员登录后自动进入商户工作台。
- 登录表单不再预填管理员账号和密码。
- 退出登录时同步清空表单实例，避免上一次输入的账号和密码再次显示。
- 统一登录接口加入匿名访问白名单，并保留原总部、商户登录接口兼容其他端。

### 2.2 商户账号新建订单权限

- 新增独立权限点 `order.create`。
- 新增账号级直接权限表 `auth_account_permission`。
- 登录后的有效权限由角色权限和账号直接权限合并计算。
- 总部后台“系统管理 > 账号管理”增加“新建订单”权限开关。
- 只有被平台管理员单独授权的商户体系账号可以新建订单或批量导入订单。
- 商户创建订单时强制校验商户归属、门店范围、门店商品、SKU 和资产范围。
- 平台账号、商户账号、消费者账号的订单创建入口增加账号类型隔离，防止跨入口绕过权限。

### 2.3 商户、门店与账号关联

- 总部“账号管理”和“员工账号”新增账号时，授权门店会随所选商户动态过滤。
- 禁止把其他商户的门店或已停用门店授权给账号。
- 商户老板账号自动继承所属商户当前及以后新增的全部门店。
- 已停用商户不能新增门店或账号，已停用门店不能继续用于账号授权。
- 停用商户或门店时，其门店商品会自动下架，避免已停用经营主体继续接单。

### 2.4 电牛哥品牌升级

> 历史记录：该品牌版本已于 2026-07-26 被第 19 节“途派熊品牌升级”替代。

- 登录页品牌统一升级为“电牛哥租赁运营平台”，使用牛哥透明 Logo 作为主视觉。
- 登录页重新规划总部、商户、门店协作说明以及门店订单、资产履约、分润结算三项业务能力展示。
- 运营后台浏览器标题、登录入口和系统侧栏统一使用“电牛哥”名称及 Logo。
- 商户端、用户端小程序名称与用户端导航标题统一调整为“电牛哥租车”。
- 合同名称、支付账单、资金授权及协议扣款中的对外展示名称同步调整为“电牛哥”。
- 保留 Java 包名、数据库名、接口路径和本地存储键中的 `xniu` 技术标识，避免破坏现有兼容性。

## 3. 商品、链接与 SKU

- 原“SKU 管理”调整为“链接管理”。
- 一个商品链接可以配置多种规格。
- 规格名称支持自定义，每个规格作为一个 SKU。
- 原“套餐管理”调整为“SKU 管理”。
- 新增和编辑时使用“SKU”名称，不再使用“套餐名称”。
- 每个 SKU 增加独立价格 `price_amount`。
- 门店商品引用商品链接及其 SKU，门店发布时同步 SKU 价格。
- 商品链接编码前缀调整为 `LINK`，SKU 编码前缀调整为 `SKU`。
- 门店商品、外部补录订单、订单导入、用户端商品展示中的“套餐”文案统一调整为“SKU”。
- 新增商户安全的门店商品查询接口，商户端不再复用总部全局商品接口。
- 新增商品链接与 SKU 集成测试，覆盖名称、价格、门店发布和订单金额。
- 商品分类、商品链接、SKU 和门店商品补齐编辑、删除入口及后端接口。
- 删除操作增加业务引用校验，有历史订单、外部订单、核销、分润或逾期数据时禁止误删。
- 已发布的商品链接禁止改变链接类型，SKU 编辑时禁止迁移到其他商品链接。
- 门店商品发布改为仅新增，禁止同一门店重复发布同一链接；编辑不再隐式重新上架。
- 门店商品批量上架限定同一商户的启用门店，并校验门店、链接、SKU、销售类型和价格关系。
- 门店商品必须先下架且不存在历史业务数据后才允许删除。

## 4. 自动续租

### 4.1 SKU 续租规则

- 门店 SKU 配置增加“自动续租”开关。
- 增加续租周期单位、续租周期值和续租金额。
- 默认按照原租期、期数和每期金额生成续租参数。
- 关闭自动续租时不保存续租周期和金额。
- 开启自动续租时校验续租周期必须大于 0、续租金额必须大于 0。

### 4.2 订单续租快照

- 订单创建时冻结自动续租开关、周期单位、周期值和续租金额。
- 订单增加续租次数 `renewal_count`。
- 商品后续修改续租规则不会反向改变历史订单。

### 4.3 自动续租执行

- 新增到期订单扫描服务 `OrderRenewalService`。
- 对到期且未归还、开启自动续租的订单生成续租账单。
- 新增续租账单类型、续租租金明细类型和续租生成批次类型。
- 避免同一订单重复生成未结清续租账单。
- 到期订单进入逾期状态并参与补缴、代扣和逾期处理。
- 续租账单支付成功后，预计归还时间按续租周期顺延，续租次数加一。
- 支付成功后如无其他到期未付账单，订单恢复租赁中状态。
- 支付宝主动支付、资金授权扣费、协议代扣成功均接入续租成功处理。
- 协议代扣失败时保留续租账单并进入待补缴/逾期流程。
- 新增总部后台手动运行自动续租扫描入口和执行结果展示。
- 续租租金进入收益台账和月结计算。

### 4.4 灵活续租价格与存量调价

- 门店 SKU 的首期/原租期价格与续租价格分离，支持“首期 99 元、以后每 30 天续租 129 元”等定价。
- 续租规则新增整期模式和按日模式；按日模式可单独设置正常日续租价、逾期日占用费、宽限小时和是否整期封顶。
- 开启封顶时，每个续租周期的按日累计金额不超过整期续租价；关闭封顶时按实际天数持续计费。
- 订单、续租账单分别保存定价快照，SKU 后续改价不会改变历史订单，订单后续调价不会改变已生成或已支付账单。
- 归还时按实际超期天数结算；已有未结清续租账单时只生成剩余差额账单，避免重复收费或漏收跨整期后的零头天数。
- 存量订单新增续租调价记录。对用户有利的降价立即生效；涨价、新增收费、缩短周期/宽限、取消封顶或切换计费模式需补充协议签署后生效。
- 合同新增主合同、续租价格补充协议及调价记录关联，原签署合同保持不可变；普通合同生成接口会拒绝误用续租调价模板。
- 总部订单页新增续租调价和调价历史，待确认记录可重试生成补充协议；账单页支持按整期或指定天数生成续租账单。
- 新增 V50、V51 迁移及专项回归测试；全新隔离库迁移至 V51 后，后端 105 项测试全部通过。

### 4.5 外部补录订单续租调价

- 外部补录订单保存独立续租价格快照，新订单从门店 SKU 冻结规则，已有补录订单由 V51 从当前门店 SKU 回填初始规则。
- 总部和商户 Web 均支持单笔续租调价、调价历史、人工客户确认和待确认后补登记。
- 支持按勾选订单、门店、状态、来源平台、门店商品、SKU、起租/预计归还时间及订单、客户、手机号、资产关键词组合筛选后批量调价。
- 批量操作必须先预览，并携带预期命中数量执行；数据范围变化时后端拒绝执行，避免筛选变化造成误批量。
- 降价、关闭自动续租等有利变更立即生效；涨价、新增收费、切换计费方式、缩短周期/宽限或取消封顶，需登记逐单人工确认，否则只生成待确认记录并保持原规则。
- 普通补录资料编辑不会覆盖订单续租价格；订单完结或提前终止时，尚未确认的调价自动取消。
- 外部补录订单仅维护续租价格台账和人工确认记录，不自动生成平台续租账单、代扣、逾期案件或电子合同。

### 4.6 好评与活动赠送租期

- 总部、商户老板和具备 `order.operate` 权限的门店人员可以为订单赠送租期。
- 赠送类型分为“好评赠送”和“活动赠送”，默认分别带出 2 天和 15 天。
- 赠送天数可按实际活动修改为 1 至 999 天，并可填写操作备注。
- 订单起租前赠送时先累计天数，取车或发货起租时统一计入预计归还时间。
- 租赁中赠送时立即顺延预计归还时间；已到期但赠送后仍在有效期内的订单恢复为租赁中。
- 赠送租期不增加订单应付、账单、续租次数或分润金额。
- 自动续租扫描使用顺延后的预计归还时间，避免赠送有效期内提前生成续租账单。
- 每次赠送均记录类型、天数、备注、操作账号、顺延前后时间和操作时间。
- 总部 Web、商户 Web、商户小程序和用户小程序均展示赠送汇总及明细。

## 5. 订单创建、批量导入与订单信息

### 5.1 订单客户信息

- 正式订单增加客户姓名 `customer_name`。
- 正式订单增加客户电话 `customer_phone`。
- 历史订单从账号资料回填客户姓名和电话。
- 订单列表和详情补充客户姓名、联系电话、门店、商品链接、SKU、车架号和电池号。

### 5.2 自定义下单时间

- 订单增加业务下单时间 `ordered_at`。
- 新建订单允许选择当前时间或过去日期。
- 历史订单使用原创建时间回填业务下单时间。
- 列表排序、展示、导入和导出统一使用业务下单时间。

### 5.3 总部与商户新建订单

- 总部后台继续支持单笔新建订单。
- 商户 Web 后台和商户支付宝小程序增加新建订单入口。
- 商户新建订单受 `order.create` 权限控制。
- 商户只能选择授权门店的上架商品、SKU 和空闲资产。
- 创建成功后自动生成完整账单计划。
- 用户账号 ID 改为可选，客户姓名和联系电话可独立录入。
- 总部新建订单的车架/车电一体资产和电池资产支持直接输入模糊搜索，可按序列号、资产编号或资产类型筛选。
- 电池资产明确调整为选填，车电一体仍会自动清空并禁用独立电池。
- 总部单笔新建订单取消预计取车时间录入，仅保留下单时间；历史订单中的预计取车数据继续正常展示。

### 5.4 订单批量导入

- 总部订单管理增加批量导入和模板下载。
- 商户订单管理增加批量导入和模板下载。
- 模板支持用户账号 ID、客户姓名、联系电话、门店商品编码、SKU 编码、车架号、电池号、预计取车时间和下单时间。
- 支持 `YYYY-MM-DD`、`YYYY-MM-DD HH:mm` 和带秒时间格式。
- 每行独立事务处理，单行失败不影响其他行。
- 返回总行数、成功数、失败数和逐行错误原因。
- 批量导入同样执行门店范围、商品、SKU、资产状态和账号权限校验。

### 5.5 订单检索、序号与导出

- 总部和商户订单列表增加序号列。
- 支持按订单号、客户姓名、联系电话、门店、商品链接、SKU、车架号和电池号搜索。
- 保留订单状态、门店和用户账号筛选。
- 总部和商户订单列表增加 CSV 导出。
- 导出内容包含客户信息、商品/SKU、资产号、金额、预计归还时间和下单时间。
- 数据库增加订单门店/状态/下单时间、客户姓名、车架和电池索引。

### 5.6 订单状态与总部履约

- 总部订单管理补齐免付款发货、取车绑定、更换资产和归还结束的专用操作入口。
- 通用状态流转只保留实名、签约、免押、核销、待取车、待还车和逾期等正常状态推进。
- 禁止通过通用状态流转绕过资产绑定直接进入租赁中，或绕过资产归还直接完成订单。
- 取消、异常、状态流转和资产履约统一执行商户及门店数据范围校验。
- 新建订单只允许选择启用商户、启用门店、已上架门店商品和启用 SKU。
- 总部订单操作按钮按当前状态显示，已完成、已取消等终态不再出现无效操作。

## 6. 未付款发货

- 商户或门店可对待支付订单选择“免付款发货”。
- 发货时直接绑定车架、电池或车电一体资产。
- 发货后订单进入租赁中状态并生成取车交接记录、资产使用记录和状态日志。
- 该流程不要求用户先完成付款。
- 商户支付宝小程序增加确认提示和“免付款发货”操作入口。
- 普通已付款取车流程仍保持原有待取车状态校验。

## 7. 核销金额

- 核销记录增加独立字段 `verification_amount`。
- 券面/参考金额与实际核销金额分开保存。
- 客户在核销准备时可填写实际核销金额，也可以稍后补录。
- 客户忘记填写时，商户、门店或平台管理员可以补录。
- 核销订单生成前允许修改；订单生成后禁止再次修改。
- 未填写实际核销金额时禁止完成验券或消费核销。
- 实际核销金额允许与 SKU 价格不同，但不能小于 0。
- 生成订单时，订单租金、SKU 明细金额和分润结算基数统一使用实际核销金额。
- 用户支付宝小程序、商户支付宝小程序和总部核销管理页均增加核销金额展示和编辑入口。

## 8. 分润规则与月结

### 8.1 PROFIT_V2 分润模型

- 分润计算升级为 `PROFIT_V2`。
- 以实际核销金额或实际结算金额作为分润基数。
- 默认先扣渠道核销扣点 5%。
- 默认再扣租赁平台扣点 3%。
- 剩余金额默认按以下比例分配：
  - 门店运营 15%。
  - 维修基金 10%。
  - 渠道引流 20%。
  - 出资方 55%。
- 新增来源渠道、规则优先级和计算版本。
- 新增渠道、平台、门店运营、维修基金、渠道引流和出资方六类收益明细。
- 分润快照完整冻结命中规则、比例和金额。
- 历史快照保留 `LEGACY_V1`，不会因新版规则上线而重算。
- 月结和收益台账适配新版分润金额及续租租金。

### 8.2 门店独立分润规则

- 分润规则从统一规则调整为每个门店独立规则。
- 已有门店按照当前有效规则自动初始化门店规则。
- 新建门店自动继承当前平台默认规则。
- 结算时门店规则优先于门店商品、商品链接和平台规则。
- 平台管理员可在“分润结算 > 门店分润规则”直接编辑每个门店的比例。
- 商户账号即使拥有结算权限，也不能修改门店分润规则。
- 门店规则修改只影响之后生成的新快照，不回写历史订单和历史月结数据。
- 删除无业务数据门店时同步清理门店分润规则。

## 9. 资产管理

### 9.1 资产批量录入

- 总部资产台账增加批量录入和模板下载。
- 商户 Web 后台增加门店资产批量录入和模板下载。
- 新增 `asset.import` 权限，默认授予平台管理员、商户老板和门店经理。
- 模板支持资产类型名称或编码、资产编号、出资方编码、门店编码、采购金额、报废残值和采购日期。
- 下载模板升级为 Excel 工作簿，分别提供“资产导入”、“填写示例”、“填写说明”和编码参考工作表。
- 模板会带入当前有效的资产类型、出资方编码和门店编码，减少因填写名称或无效编码导致的导入失败。
- 上传同时支持 `xlsx`、`xls`、`csv`、`tsv` 和文本文件，并兼容中文 Excel/WPS 常见的 GB18030 编码 CSV。
- 表头允许前置说明行和常见全角符号；门店编码、报废残值和采购日期列可缺省。
- 支持系统资产类型、自定义资产类型、中文名称和英文枚举值。
- 商户导入时锁定当前门店，禁止跨门店导入。
- 每行独立事务，返回逐行成功或失败原因。

### 9.2 入库字段调整

- 取消资产入库时手工填写固定维保费。
- 新资产固定维保费按 0 保存。
- 报废残值调整为非必填，可保存为空。
- 采购金额仍为必填项。

### 9.3 车电一体资产

- 新增资产类型 `INTEGRATED_VEHICLE`。
- 车电一体资产只绑定车架号，不需要独立电池号。
- 订单创建、批量导入、正式履约、免付款发货、外部补录订单和资产更换均支持车电一体。
- 选择车电一体后自动清空并禁用独立电池选择。
- 从普通车架切换到车电一体时自动解绑原电池并关闭电池使用记录。
- 资产使用轨迹和出资方收益归属支持车电一体资产。

### 9.4 资产检索、序号与导出

- 总部和商户资产列表增加序号列。
- 支持按资产编码、车架号、电池号、出资方、商户和门店搜索。
- 保留资产类型、状态、门店等筛选。
- 总部和商户资产列表增加 CSV 导出。
- 数据库增加资产类型/状态/门店和商户/状态联合索引。

### 9.5 归还门店约束

- 正式订单和外部补录订单归还时均校验归还门店必须启用且属于原商户。
- 跨门店归还必须由商品链接明确开启“支持跨店归还”，否则前后端同时拦截。
- 跨店归还成功后自动调拨资产到实际归还门店并记录位置变化。

### 9.6 自定义资产类型与门店自主录入

- 新增可维护的资产类型库，总部管理员可以新增和编辑类型名称、编号字段名称、排序和启停状态。
- 每个自定义类型必须归入车架、电池、车电一体或普通资产四类业务归类之一。
- 普通资产不会进入订单车架、电池选择器，避免头盔、充电器等资产被误用于订单履约。
- 已有资产自动关联到车架、电池、车电一体三个系统类型，历史数据无需重新录入。
- 资产台账支持编辑类型、资产编号、出资方、采购金额、残值和采购日期。
- 已有资产只能切换到相同业务归类的类型，租赁中的资产禁止编辑，避免影响正在履约的订单。
- 新增 `asset.manage` 权限，默认授予平台管理员、商户老板和门店店长。
- 商户老板和门店店长可以在门店资产页单笔新增、编辑资产，资产归属由后端固定到当前授权门店。
- 门店账号不能给无权限门店新增资产，也不能编辑其他门店的资产。

## 10. 外部补录订单兼容调整

- 外部补录订单中的“套餐”文案调整为“SKU”。
- 商户端改用安全的商户门店商品查询接口。
- 外部补录订单支持绑定车电一体资产。
- 车电一体仅填写车架资产，电池资产留空。
- 批量补录说明同步更新 SKU 和车电一体规则。
- 外部订单资产校验统一使用商品链接的资产需求配置。
- 外部补录订单增加整期续租价、日租价、逾期日占用费、宽限小时和封顶规则快照。
- 外部补录订单页面增加门店商品、SKU、起租/预计归还时间筛选、表格勾选、单笔调价、调价历史和按条件批量调价。
- 批量调价支持预览统计、逐单处理结果、客户人工确认留痕和门店权限隔离。

## 11. 各端页面变化

### 11.1 总部管理后台 `admin-web`

- 登录页统一运营入口。
- 系统账号管理增加账号级新建订单权限开关。
- 商品管理调整为链接管理、SKU 管理和门店商品。
- 订单管理增加客户信息、资产号、批量导入、模板、自定义下单时间、搜索、序号和导出。
- 资产管理增加批量导入、模板、车电一体、可选残值、自定义类型、资产编辑、搜索、序号和导出。
- 核销管理增加实际核销金额展示和补录。
- 分润结算增加新版分润预览、门店独立规则编辑和新版快照字段。
- 自动扣款页增加自动续租扫描操作。
- 账单、出资方、履约、外部订单等页面适配续租、SKU、车电一体和新版分润字段。

### 11.2 商户 Web 工作台

- 商户工作台首页按账号权限加载收益和月结数据，无 `settlement.read` 权限的门店角色不再因分润接口 403 导致整个首页加载失败。
- 商户老板和门店店长可查看分润数据；门店运营、门店员工、维修人员和仓库人员隐藏待结算收益、最近月结金额和月结单区域。
- 按账号权限显示新建订单入口。
- 支持单笔新建订单、批量导入和模板下载。
- 支持自定义历史下单时间。
- 支持订单搜索、序号和 CSV 导出。
- 支持资产单笔新增、编辑、批量导入、模板、搜索、序号和 CSV 导出。
- 商户老板和门店店长可将资产直接录入当前授权门店，跨门店操作由后端拦截。
- 商品、订单、履约、收益页面适配 SKU、车电一体、自动续租和新版分润。

### 11.3 商户支付宝小程序

- 按 `order.create` 权限显示新建订单。
- 支持选择门店商品、SKU 和空闲资产创建订单。
- 支持车电一体资产。
- 增加免付款发货。
- 增加核销金额补录。
- 订单和分润展示适配客户信息、自动续租和 PROFIT_V2。

### 11.4 用户支付宝小程序

- 核销准备时可填写实际核销金额。
- 未填写时可稍后补录。
- 核销记录显示参考金额和实际核销金额。
- 订单和商品展示适配 SKU 名称、价格及新版字段。

## 12. 数据库迁移

| 版本 | 文件 | 内容 |
| --- | --- | --- |
| V27 | `V27__voucher_verification_amount.sql` | 增加实际核销金额并补充商户核销权限 |
| V28 | `V28__auto_renewal_schema.sql` | 增加 SKU 和订单自动续租字段 |
| V29 | `V29__merchant_order_create_permission.sql` | 增加 `order.create` 和账号直接权限表 |
| V30 | `V30__profit_sharing_v2.sql` | 增加新版六项分润规则和版本化快照 |
| V31 | `V31__order_customer_and_asset_display.sql` | 增加订单客户姓名和联系电话 |
| V32 | `V32__asset_batch_import_permission.sql` | 增加资产批量导入权限 |
| V33 | `V33__order_business_time.sql` | 增加业务下单时间 |
| V34 | `V34__optional_asset_residual_value.sql` | 报废残值调整为可空 |
| V35 | `V35__product_link_sku_price.sql` | 增加 SKU 价格并迁移原门店价格 |
| V36 | `V36__asset_order_search_indexes.sql` | 增加资产与订单检索索引 |
| V37 | `V37__store_profit_rules.sql` | 初始化每个门店的独立分润规则 |
| V38 | `V38__order_lease_bonus.sql` | 增加好评与活动赠送租期记录及权限 |
| V39 | `V39__store_manager_settlement_read.sql` | 仅为门店店长补充分润数据查看权限 |
| V40 | `V40__asset_type_management.sql` | 增加资产类型库、资产类型关联及 `asset.manage` 权限 |

## 13. 新增主要代码文件

### 前端

- `admin-web/src/components/AssetBatchImportModal.tsx`
- `admin-web/src/components/OrderBatchImportModal.tsx`
- `admin-web/src/utils/csv.ts`

### 资产

- `AssetBatchImportRequest.java`
- `AssetBatchImportResponse.java`
- `AssetBatchImportRowRequest.java`
- `AssetBatchImportRowResultResponse.java`
- `AssetTypeDefinition.java`
- `AssetTypeRequest.java`
- `AssetTypeResponse.java`
- `AssetUpdateRequest.java`
- `AssetInvestorOptionResponse.java`
- `AssetTypeRepository.java`
- `AssetTypeService.java`
- `AdminAssetTypeController.java`

### 账号权限

- `SystemAccountPermissionUpdateRequest.java`

### 订单与续租

- `AdminOrderRenewalController.java`
- `OrderBatchImportRequest.java`
- `OrderBatchImportResponse.java`
- `OrderBatchImportRowRequest.java`
- `OrderBatchImportRowResultResponse.java`
- `RenewalRunRequest.java`
- `RenewalRunResponse.java`
- `OrderBatchImportService.java`
- `OrderCreationService.java`
- `OrderRenewalService.java`
- `OrderLeaseBonusRequest.java`
- `OrderLeaseBonusResponse.java`
- `OrderLeaseBonus.java`
- `OrderLeaseBonusType.java`
- `V38__order_lease_bonus.sql`

### 商品

- `MerchantProductController.java`

### 分润

- `StoreProfitRuleUpdateRequest.java`
- `SettlementCalculationVersion.java`
- `ProfitSharingCalculator.java`

### 核销

- `VoucherVerificationAmountRequest.java`

## 14. 测试变化

- 新增 `AssetBatchImportIntegrationTests`。
- 新增 `AuthWorkspaceLoginIntegrationTests`。
- 新增 `MerchantOrderCreationPermissionIntegrationTests`。
- 新增 `OrderBatchImportIntegrationTests`。
- 新增 `OrderLeaseBonusIntegrationTests`，覆盖起租前赠送、租赁中顺延、自动续租避让及门店权限隔离。
- 新增 `ProductLinkSkuIntegrationTests`。
- 新增 `ProfitSharingCalculatorTests`。
- 扩展 `RentalBusinessFlowIntegrationTests`，覆盖：
  - 未付款发货。
  - 自动续租账单生成。
  - 协议代扣失败与成功续租。
  - 核销金额与 SKU 价格不同。
  - 车电一体履约。
  - 新版分润和门店规则。
  - 历史快照冻结。
- 新增商品分类、链接、SKU、门店商品的完整编辑/删除/上下架生命周期测试。
- 新增商户账号与门店授权关系测试，覆盖跨商户门店和停用门店拦截。
- 新增订单门店权限测试，防止商户账号跨门店取消或操作订单。
- 新增订单状态绕过测试，确保起租和完成必须经过资产履约接口。
- 新增跨店归还测试，覆盖链接开关、停用门店和成功调拨。
- 扩展资产集成测试，覆盖自定义类型新增/编辑、普通资产入库、门店店长单笔新增/编辑和跨门店拦截。
- 扩展外部订单、系统管理和门店删除测试。

## 15. 当前验证结果

- 后端：`mvn test`，57 个测试全部通过。
- 总部/商户 Web：`npm run build` 通过。
- 商户支付宝小程序：`npm run typecheck`、`npm run build:mp-alipay` 通过。
- 用户支付宝小程序：`npm run typecheck`、`npm run build:mp-alipay` 通过。
- Flyway：全新测试库已完整执行 V1 至 V40。
- 本地后端：`http://127.0.0.1:8090/actuator/health` 返回 `UP`。
- 本地前端：`http://127.0.0.1:5173/` 可正常访问。
- 已人工验证：
  - 订单和资产搜索、序号与导出入口。
  - 门店分润规则列表、新增、编辑、启停和删除保护。
  - 分润预览命中门店规则。
  - 统一登录页无总部入口且账号密码为空。
  - 管理员从“商户登录”进入总部管理后台。
  - 门店员工分别执行好评赠送 2 天和活动赠送 15 天，订单累计、明细记录和权限均正确。
  - 系统账号和商户员工新增表单会按所选商户正确列出授权门店。
  - 链接、SKU、门店商品均可进入编辑，删除和上下架按钮状态符合业务约束。
  - 管理员对演示订单执行免付款发货，订单进入租赁中且 17 天赠送自动计入预计归还。
  - 租赁中追加活动赠送 1 天后预计归还即时顺延，订单赠送合计变为 18 天。
  - 管理员归还订单后订单进入已完成，车架和电池恢复空闲。
  - 订单按联系电话和车架号检索、自定义历史下单时间、模板下载和批量导入入口正常。
  - 总部新建订单可直接输入车架号、资产编号或类型模糊搜索资产，电池可留空，且不再显示预计取车时间。
  - 退出登录后账号与密码输入框为空。
  - 门店店长登录后可查看所属授权门店的分润数据，其他门店角色不显示且不能访问分润数据。
  - 电牛哥登录页在桌面与 390px 移动视口下 Logo、标题、业务能力和登录表单均正常显示，无横向溢出。
  - 总部资产台账可打开类型管理，查看三个系统类型并进入自定义类型新增、编辑表单。
  - 总部空闲资产可进入基础资料编辑，租赁中资产编辑按钮自动禁用。
  - 商户门店资产页显示单笔新增入口，类型、编号、出资方、金额和日期字段加载正常，接口无 403。
  - V40 已将现有资产全部关联到类型库，`asset_type_id` 无空值；平台管理员、商户老板和门店店长均已获得 `asset.manage` 角色权限。

## 16. 完整文件范围

### `admin-web`（23 个文件）

- `index.html`
- `public/dianniuge-mascot.png`（已删除）
- `public/tupaixiong-logo.png`
- `src/App.tsx`
- `src/components/AssetBatchImportModal.tsx`
- `src/components/OrderBatchImportModal.tsx`
- `src/pages/AgreementDeductManagement.tsx`
- `src/pages/AssetFulfillmentManagement.tsx`
- `src/pages/AssetManagement.tsx`
- `src/pages/BillManagement.tsx`
- `src/pages/ComplianceManagement.tsx`
- `src/pages/ExternalOrderManagement.tsx`
- `src/pages/InvestorWorkspace.tsx`
- `src/pages/MerchantManagement.tsx`
- `src/pages/MerchantWorkspace.tsx`
- `src/pages/OrderManagement.tsx`
- `src/pages/ProductManagement.tsx`
- `src/pages/SettlementManagement.tsx`
- `src/pages/SystemManagement.tsx`
- `src/pages/VoucherManagement.tsx`
- `src/types/api.ts`
- `src/utils/csv.ts`
- `src/styles.css`
- `tsconfig.tsbuildinfo`

### `merchant-mini`（3 个文件）

- `src/manifest.json`
- `src/pages/index/index.vue`
- `src/types/api.ts`

### `user-mini`（4 个文件）

- `src/manifest.json`
- `src/pages.json`
- `src/pages/index/index.vue`
- `src/types/api.ts`

### `docs`（3 个文件）

- `docs/开发进度.md`
- `docs/系统管理权限体系设计.md`
- `docs/feature-content-updates-changelog.md`

### `server/rental-api`（132 个文件）

- 资产模块：控制器、批量导入 DTO、资产类型、仓储、入库/调拨/履约/维修服务。
- 认证模块：登录控制器、账号权限 DTO、权限查询、系统管理仓储、账号类型隔离、统一登录和直接权限服务。
- 账单模块：续租账单类型、明细类型、生成批次、仓储和账单服务。
- 外部订单模块：创建 DTO 和资产/SKU 校验服务。
- 出资方模块：资产查询适配车电一体和新版收益。
- 商户模块：门店规则自动初始化及删除处理。
- 订单模块：控制器、创建 DTO/响应/模型、仓储、创建服务、批量导入、自动续租和赠送租期。
- 支付模块：协议代扣、资金授权、主动支付接入续租成功处理。
- 商品模块：链接、SKU、门店商品、价格和续租配置。
- 分润模块：PROFIT_V2 计算、门店规则、快照、收益和月结。
- 核销模块：实际核销金额的三端录入、校验、订单生成和分润基数。
- 数据库：V27 至 V41 共 15 个迁移。
- 测试：7 个新增测试文件及 4 个扩展测试文件。

以上范围覆盖当前 `feature/content-updates` 分支相对 `main` 的全部业务改动。

## 17. 实际核销金额与店长新建订单补全（2026-07-22）

- 正式订单新增独立的“实际核销金额”：总部 Web、商户 Web、商户小程序和批量导入均可录入；选择 SKU 后默认带出参考价，但允许修改。
- 实际核销金额写入订单、应付金额、订单明细和分润快照，分润计算不再误用 SKU 参考价；旧客户端未传值时仍兼容回退到 SKU 价格。
- 外部补录订单新增独立的“实际核销金额”，创建、批量导入、列表和详情均已补齐，并与外部租赁金额分别保存。
- `STORE_MANAGER` 默认获得 `order.create` 权限，可以为授权门店新增订单；其他门店角色不自动开放，服务端仍校验门店与资产归属。
- 后端全量测试 57 个全部通过；总部/商户 Web 构建、商户与用户小程序类型检查及支付宝构建均通过。
- 页面人工核对通过：店长账号显示并可打开“新建订单”，总部新建订单和外部补录单弹窗均显示“实际核销金额”。

## 18. 门店分润规则新增与完整管理（2026-07-22）

- “分润结算 > 门店分润规则”增加新增规则入口，并支持按门店、渠道和状态筛选。
- 新增规则可配置规则名称、适用门店、适用渠道、优先级、生效时间、失效时间及六项分润比例。
- 规则列表增加规则编码、渠道、优先级、有效期、状态，以及编辑、启用/停用、删除操作。
- 新增完整规则编辑接口；门店和商户归属由服务端根据所选门店自动确认，不信任前端传入的归属数据。
- 创建、编辑、启停和删除均只允许平台账号执行，商户账号即使拥有 `settlement.write` 也不能越权操作。
- 每个门店必须保留一条当前生效的全部渠道默认规则；已生成分润快照的规则不能删除，但可以停用，历史快照不受后续规则调整影响。
- 新增 3 个集成测试，覆盖规则新增命中、编辑、启停、删除、默认规则保护、快照保护和商户越权拦截。
- 后端全量 57 个测试通过，管理后台生产构建通过，并已在本地页面完成新增及删除规则的实际操作验证。

## 19. 途派熊品牌升级（2026-07-26）

- 对外品牌由“电牛哥”统一升级为“途派熊”，运营后台名称调整为“途派熊租赁运营平台”。
- 使用新的途派熊透明 Logo 替换原品牌形象，浏览器图标、登录页主视觉、登录入口和系统侧栏同步更新。
- 商户端、用户端小程序名称与用户端导航标题统一调整为“途派熊租车”。
- 合同名称、支付账单、资金授权及协议扣款中的客户可见名称同步调整为“途派熊”。
- 保留 Java 包名、数据库名、接口路径、部署容器名和本地存储键中的 `xniu` 技术标识，确保现有数据与接口兼容。

## 20. 管理员分润结算工作台优化（2026-07-26）

- 管理员“分润结算”重构为月结中心、分润快照、门店规则和收益台账四个独立工作区，减少长页面堆叠。
- 月结中心增加月份切换、汇总指标、状态进度、商户/门店/出资方名称映射、组合筛选、CSV 导出和完整状态推进。
- 已确认、待打款、已打款或已关闭的月份自动锁定，避免重新生成覆盖历史月结；月结详情可按明细类型和关键字核对并导出。
- 分润快照增加来源、门店、渠道、计算版本筛选，详情展示实际核销金额、两项扣点、剩余可分配金额和四方分配，并自动检查金额平衡。
- 门店规则增加启用规则数、默认规则覆盖率和缺失门店提醒；规则测算支持全部订单渠道，历史快照仍锁定原规则和计算结果。
- 收益台账增加正式订单/补录订单来源筛选、按名称展示收益方、筛选后汇总及导出；历史补生成改为按订单号、客户和门店搜索选择。
- 本次仅优化管理员操作与追溯能力，不调整当前分润公式和已落库的历史结算数据。
- 管理后台类型检查与生产构建通过，后端全量 84 个测试通过；本地管理员页面已完成四个工作区切换、规则新增表单和 1000 元实际核销金额测算验收。

## 21. 各门店实际分润总览（2026-07-27）

- 管理员分润结算新增“门店分润”工作区，以正式月结单为唯一实际结算口径，避免把未收账单或预计收益当作实际分润。
- 每个门店独立展示实际核销/实收基数、实收签单费、门店运营分润、门店维修分润、维修补回、维保扣减、人工调整和最终应结算金额。
- 增加已打款、待结算、尚未生成月结等状态，并将所有门店纳入列表；没有月结单的门店会明确提示进入月结中心生成。
- 支持按月份、商户、门店、月结状态和关键字筛选，汇总指标随筛选结果变化，并支持门店分润 CSV 导出。
- 门店行可直接打开对应月结单及明细，继续追溯到正式订单账单、补录订单、资产和维修调整记录。
- 后端增加门店月结汇总接口，从月结明细中分别汇总门店运营与门店维修金额，保证总览与月结单使用同一数据源。
- 门店分润汇总定向测试及后端全量 84 个测试通过，管理后台类型检查与生产构建通过；本地管理员页面已验证未生成月结提示、全门店列表、月结中心联动和导出入口。

## 22. 出资方运营后台完善（2026-07-27）

- 出资方工作台增加资产账面投入、参考残值、投放率、已结算收益、待归集收益和待打款月结等经营指标。
- 增加门店投放分布、资产运营状态、经营关注、最近月结单和最近收益流水，便于出资方发现空闲、维修、异常、未分配和冻结数据。
- “我的资产”升级为“资产运营”，支持资产编码、序列号、自定义类型和门店模糊搜索，以及类型、门店、状态筛选、分页和 CSV 导出。
- 资产详情增加租赁记录数、累计已收、累计实际核销、维修记录金额，并完整展示租赁账单和维修配件明细。
- “收益分成”升级为“收益结算”，拆分收益流水与月结对账，增加状态、来源、类型、月份和关键字筛选，以及流水、月结单和月结明细导出。
- 出资方页面继续隐藏订单录入时间和收益发生时间，仅保留租赁履约、结算确认与实际打款等业务时间；当前规则下日常维修不从出资方收益扣减。
- 管理后台类型检查与生产构建通过；演示出资方账号已完成工作台、资产模糊搜索、资产详情、收益流水和月结对账浏览器验收，无 403 和新增运行时错误。

## 23. 旧版 Safari 兼容与手机端适配（2026-07-27）

- 管理后台增加旧浏览器兼容构建，为 Safari 11、iOS 11 及以上设备生成独立 `nomodule` 脚本和运行时 polyfill，同时保留现代浏览器模块包。
- 移除手机端原有的 `1180px` 强制最小宽度，桌面侧栏在窄屏下改为可开关的抽屉导航。
- 手机页头重新布局，商户门店选择器、账号入口、页面标题和导航按钮可在窄屏内共同显示。
- 指标、表单和页面分栏在手机端纵向重排；数据表格、详情表和描述表使用局部触摸横滑，避免整页横向溢出。
- 弹窗在手机端限制为视口宽度并提供内部滚动，筛选项和主要操作按钮按窄屏宽度重新排列。
- 生产 Nginx 将 `index.html` 设置为不缓存、带哈希的 `/assets/` 资源设置长期缓存，避免旧设备继续引用已删除脚本导致白屏。
- 移除未使用且存在公开安全告警的 `react-router-dom` 依赖；生产依赖审计为 0 项漏洞。
