# 享牛科技租赁平台产品规格

## 1. 文档信息

- 当前版本：`v1.1.0`
- 更新日期：`2026-09-02`
- 适用系统：总部管理后台、商户管理后台、租赁 API、结算中心
- 本次范围：补录订单一次性人工续租、外卖车电池成本清分与毛额级 20% 渠道分润

## 2. 目标与问题

补录订单可能由运营人员在线下完成续租，再回到平台登记实际续租截止时间和核销金额。现有系统把月续租固定为 30 天，并将人工核销金额作为没有结束边界的长期金额时间线，无法准确表达“一次人工续租覆盖一段精确租期”。这会造成租期重叠或空档、续租金额套用错误，以及外卖车电池成本与实际租期不一致。

本次目标：

1. 将一次人工续租保存为具有明确起点、终点和毛核销金额的独立事件。
2. 人工续租不再使用固定 30 天推导租期。
3. 外卖车先按核销毛额计算 5% 渠道费、3% 平台费和 20% 渠道引流分润，再扣除该租期全部电池成本。
4. 扣除上述项目后的余额按 15:10:55 权重重新分配给门店运营、门店维修和出资方，不将全部差额单独转给某一方承担。
5. 保证租期连续、金额守恒，并对所有外卖车有效补录订单及续租进行可审计的历史回算。

## 3. 名词与金额口径

- **当前已付至时间**：补录订单当前的 `expectedReturnAt`，也是下一次人工或自动续租的起点。
- **人工续租终点**：运营人员为本次续租精确选择的新到期时间，精确到秒。
- **人工核销毛额**：运营人员输入的本次实际核销金额，例如 ¥96、¥239；该金额为扣费前毛额，不是商户到账净额。
- **一次性人工续租**：只覆盖本次明确起止时间的续租事件，不自动覆盖后续周期。
- **自动续租默认规则**：没有人工续租时，订单继续使用已生效的 `renewalUnit / renewalValue / renewalAmount` 生成后续周期。

## 4. 核心业务规则

### 4.1 人工续租时间

一次人工续租必须满足：

```text
periodStartAt = 操作前订单 expectedReturnAt
periodEndAt   = 人工输入的新到期时间
periodEndAt   > periodStartAt
```

- 起点不得使用管理员点击时间或客户确认时间。
- 人工续租成功后，订单 `expectedReturnAt` 原子推进到 `periodEndAt`。
- 人工续租事件必须首尾相接，不允许与已有续租事件重叠，也不允许跳过当前已付至时间形成空档。
- 管理员操作时间、客户确认时间只用于审计，不改变租期边界。
- 普通订单编辑不得为已开始订单直接延长租期；增加租期统一使用人工续租入口。

### 4.2 人工续租金额

- 人工核销毛额只覆盖本次人工续租事件。
- 本次事件结束后，如果没有新的人工续租，后续自动续租恢复使用订单当前已生效的系统默认规则。
- 人工毛额不按固定 30 天折算，也不根据管理员点击时间与系统价做时间加权。
- 办单费只在首期产生；人工续租不重复产生办单费。

### 4.3 分润顺序

设本次人工续租毛额为 `B`：

```text
渠道核销费 C = round2(B × channelFeeRate)
平台服务费 P = round2(B × platformFeeRate)
渠道引流   R = round2(B × channelReferralRate)
电池成本   E = 本次精确租期电池成本
三方余额   D = round2(B - C - P - R - E)
三方权重   W = storeOperationRate + maintenanceFundRate + investorShareRate
门店运营   O = round2(D × storeOperationRate / W)
门店维修   M = round2(D × maintenanceFundRate / W)
出资方     I = round2(D - O - M)
```

默认 `channelReferralRate = 20%`，与渠道 5%、平台 3% 一样直接以 `B` 为基数，电池成本不得减少该 20% 分润。默认三方权重为 `15:10:55`，权重合计 80；它们表示对 `D` 的相对分配权重，不是直接对 `D` 再乘 15%、10%、55%后留下未分配金额。

首期、人工续租和系统续租在生成分润快照前都必须校验 `D >= 0` 且 `W > 0`。毛额不足以覆盖 5%、3%、20% 和全租期电池成本时，拒绝生成事件并提示缺口；不得通过少扣电池成本或生成负分润来强行入账。

金额守恒必须满足：

```text
B = C + P + R + E + O + M + I
```

### 4.4 外卖车电池成本

所有配置了电池日成本或月成本的 SKU，必须按事件精确起止时间计算完整电池成本，并在四方分润前扣除。

电池成本配置继续保留“30 天月价阶梯”，但该 30 天仅用于成本计价，不用于推导租期：

```text
持续时长 = periodEndAt - periodStartAt（精确到秒）
每满 30 天成本 = batteryCostMonthlyAmount
剩余时长成本   = batteryCostDailyAmount × 剩余秒数 ÷ 86400
最终电池成本   = round2(月价阶梯成本 + 剩余时长成本)
```

示例：

- 20 天，日成本 ¥6.80：电池成本 ¥136.00。
- 20.5 天，日成本 ¥6.80：电池成本 ¥139.40。
- 31 天，月成本 ¥200、日成本 ¥6.80：电池成本 ¥206.80。

电池成本只扣一次；结算单可单独展示应付电池公司金额，但不得再次从门店收益中重复扣除。

## 5. 操作流程

### 5.1 管理员或有权限商户

1. 在进行中的补录订单点击“人工续租”。
2. 系统只读展示当前已付至时间。
3. 操作人输入新到期时间、本次核销毛额和必填的核销凭证/备注。
4. 页面提示金额为毛额，并提示渠道费、平台费及外卖车电池成本会在分润前扣除。
5. 请求同时提交页面打开时展示的续租起点；后端锁定订单后必须与数据库当前已付至时间逐秒一致，不一致则要求刷新重试。
6. 后端锁定续租起点所属月结月份；已确认、待付款、已付款或已关闭时拒绝直接补记，已有草稿时在同一事务内重建草稿。
7. 同一事务内创建人工续租事件、分润快照、收益流水，并推进订单到期时间。
8. 页面刷新订单详情与续租记录。

### 5.2 自动续租

- 自动续租与人工续租必须竞争同一订单锁。
- 人工续租完成后，自动续租从新的 `expectedReturnAt` 开始，不得补记已由人工事件覆盖的期间。
- 自动续租仍使用订单当前已生效的默认周期和系统续租价。

## 6. 权限与审计

- 复用补录订单操作权限及门店数据权限。
- 人工续租事件记录来源 `MANUAL`、操作账号、备注和创建时间。
- 系统自动续租事件记录来源 `SYSTEM`。
- 收益已冻结、已结算或月结已锁定的历史事件不可被普通编辑覆盖。

## 7. 历史数据处理

- 所有当前被外卖车补录首期或有效续租事件引用的 `PROFIT_V2` 快照都必须回算为 `PROFIT_V3`，不以订单是否已完结区分。
- 回算时新建 V3 快照并切换订单/续租事件当前指针，原 V2 快照保留为不可变审计记录；被历史编辑替换且已无业务指针的孤立快照不重新激活。
- 快照、门店运营、门店维修、渠道引流、出资方收益流水和快照指针必须在同一迁移中一致更新。
- 每条回算保留新旧快照 ID、新旧四方金额、迁移原因和迁移时间，以支持差异查询和回滚。
- 本次生产预检为 58 条当前有效外卖车快照（首期 38、续租 20），关联收益全为 `PENDING`且无月结明细；上线时再次预检，不符合条件时停止迁移而不做部分覆盖。
- 待客户确认的续租调价不视为已生效规则；系统迁移生成的调价记录不视为人工业务意图。

## 8. 验收标准

1. 无电池成本的订单当前已付至 8 月 1 日，人工续租至 8 月 21 日、毛额 ¥96，应生成唯一事件 `8 月 1 日—8 月 21 日 / ¥96`。
2. 事件生成后订单到期时间为 8 月 21 日；下一次自动续租从 8 月 21 日开始并恢复系统默认价格。
3. 任何并发的人工/自动续租都不能产生重叠事件。
4. 毛额 ¥96、默认费率且无电池成本时，渠道费 ¥4.80、平台费 ¥2.88、可分配 ¥88.32。
5. 外卖车 20 天人工续租电池成本为 ¥136.00；31 天为 ¥206.80，但 20% 渠道引流分润始终按毛额计算，不被电池成本稀释。
6. 默认费率下，毛额 ¥399、电池成本 ¥200 应得：5% ¥19.95、3% ¥11.97、20% ¥79.80、三方余额 ¥87.28；运营 ¥16.37、维修 ¥10.91、出资方 ¥60.00。
7. 分润明细加三项毛额级分润/扣费和电池成本后必须与毛额守恒；三方余额不足时不得产生事件或负分润。
8. 办单费不在人工续租中重复生成。
9. 毛额不足以覆盖渠道核销扣点、平台服务费、毛额级 20% 渠道引流分润和电池成本时，人工续租必须失败且不推进订单到期时间。
10. 管理端构建、后端测试及生产只读验收全部通过。
11. 页面打开后若订单到期时间被其他人工或自动续租推进，旧页面提交必须失败，不能静默缩短本次计费时长。
12. 续租起点所属月份已经锁定时不得落库；该月尚为草稿时，新续租必须立即出现在重建后的月结明细中。
13. 生产 58 条有效外卖车快照全部切换为 V3，原 V2 仍可追溯；新四方流水合计与 V3 快照逐分一致。
14. 管理端规则测算必须提供并展示本次实际租期电池成本输入项，录入值在预览及保存快照时都必须纳入计算。

## 9. 变更记录

### v1.1.0（2026-09-02）

- 纠正外卖车 20% 渠道引流分润的优先级：与 5%、3% 同以核销毛额计算。
- 明确扣除 5%、3%、20% 和全租期电池成本后，余额按 15:10:55 权重重分配给门店运营、门店维修和出资方。
- 确认所有当前有效外卖车首期和续租历史均回算为 V3，老 V2 快照保留审计。
- 管理端规则测算支持录入本次实际电池成本，避免以零成本预览外卖车收益。

### v1.0.0（2026-09-02）

- 固化补录订单一次性人工续租的精确时间与毛额口径。
- 明确外卖车先扣全租期电池成本再分润。
- 明确历史锁定账务不自动回写。
